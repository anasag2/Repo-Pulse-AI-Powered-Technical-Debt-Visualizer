kind = "web"
previewPath = "/"
title = "Repo-Pulse"
version = "1.0.0"
id = "artifacts/repo-pulse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18279

[services.development]
run = "pnpm --filter @workspace/repo-pulse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/repo-pulse", "run", "build" ]
publicDir = "artifacts/repo-pulse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18279"
BASE_PATH = "/"
